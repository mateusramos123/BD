[project]
name = "BD"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "mateusramos123", email = "mateus.oliveira.ifc@gmail.com"},
]
dependencies = ["django>=5.2.3"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
